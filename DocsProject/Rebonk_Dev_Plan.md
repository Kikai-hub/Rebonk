# Rebonk — План разработки для Claude Code

Этот план построен на основе `Rebonk_GDD.md`. Идти строго по этапам — каждый следующий этап опирается на рабочий результат предыдущего. Внутри этапа пункты можно переставлять местами, но не перескакивать через этапы целиком: сначала должен быть играбельный вертикальный срез, потом — расширение контента и полировка.

Формат каждого этапа: **Цель → Задачи → Критерий готовности (Definition of Done)**. Этап считается закрытым, только когда критерий готовности выполнен и билд собирается/играется без критичных багов.

---

## Этап 0 — Инициализация проекта

**Цель:** Рабочий пустой проект с правильной конфигурацией под мобильный ландшафтный 2D-проект.

**Задачи:**
- Создать проект Unity 6.5 (6000.5.2f1), 2D (URP) template
- Настроить Player Settings: ориентация экрана — Landscape Left/Right (запретить Portrait), таргет-платформы iOS + Android
- Настроить Build Settings под Android и iOS (даже без реального билда на устройство на этом этапе)
- Продумать и создать структуру папок проекта (Scripts, Prefabs, Art, Audio, ScriptableObjects, Scenes и т.д.) — зафиксировать конвенцию именования
- Настроить систему контроля версий (.gitignore под Unity)
- Настроить базовый пайплайн для пиксель-арта: Pixel Perfect Camera (пакет), настройки импорта спрайтов (Point filter, сжатие под мобильные)

**Definition of Done:** Пустой проект открывается, собирается в тестовый билд под Android без ошибок, камера и разрешение экрана корректно ведут себя в ландшафтной ориентации.

---

## Этап 1 — Вертикальный срез: движение + один вид оружия + враги

**Цель:** Минимальный играбельный цикл на одной сцене без UI-полировки — только чтобы механика "работает и это весело".

**Задачи:**
- Player controller: движение по виртуальному джойстику (реализовать джойстик как переиспользуемый UI-компонент)
- Камера следует за игроком
- Один тестовый персонаж (плейсхолдер-спрайт допустим) со статами: HP, скорость, урон
- Один вид оружия (например, melee-арка) с авто-таргетингом ближайшего врага
- Простой спавнер врагов: один тип врага, спавн по таймеру по периметру вне экрана, движение к игроку
- Урон при контакте враг↔игрок, смерть врага при обнулении HP
- Базовый object pooling для врагов сразу (не откладывать оптимизацию на потом — тут закладывается архитектурный паттерн для всего проекта)

**Definition of Done:** Можно запустить сцену, водить персонажа джойстиком, оружие автоматически бьёт врагов, враги умирают, спавнятся новые.

---

## Этап 2 — Прогрессия внутри забега (XP, левел-ап, апгрейды)

**Цель:** Замкнуть базовый roguelite-цикл: убийства → опыт → выбор апгрейда → усиление.

**Задачи:**
- Гемы опыта, дропающиеся с врагов, с притягиванием в радиусе подбора (магнит)
- Система уровня персонажа и кривая необходимого опыта для левел-апа
- UI экрана левел-апа: пауза игры, показ 3–4 случайных карточек апгрейда
- ScriptableObject-архитектура для описания апгрейдов (оружие/пассивка) — чтобы дальше легко добавлять контент без правки кода
- Добавить минимум 2–3 вида оружия и 2–3 пассивных предмета для проверки системы выбора и балансировки
- Лимит слотов оружия/предметов (зафиксировать число, например 6+6)

**Definition of Done:** Полный цикл "играю → леплюсь → выбираю апгрейд → игра продолжается с учётом апгрейда" работает без крашей, апгрейды реально влияют на статы/поведение оружия.

---

## Этап 3 — Таймер зоны, фаза выживания и враги-духи

> **Пересмотрено 2026-09-19.** Первая реализация делала фазу духов 60-секундным отсчётом, после которого зона считалась пройденной. Это неверно: фаза духов — бессрочное выживание, а переход дальше даёт только портал убитого босса (этап 6). Код этапа 3 приведён в соответствие (см. «Статус» ниже).

**Цель:** Реализовать структуру одного уровня (10–12 минут + бессрочная фаза выживания с духами) согласно GDD.

**Задачи:**
- Таймер зоны на 10–12 минут, видимый в HUD
- По истечении таймера — переход в фазу выживания (обычный спавн прекращается)
- Спавн врагов-духов на фазе выживания: волны со всех сторон экрана
- Механика нарастающей силы духов **по секундам** фазы (не по волнам, а от прошедшего времени внутри фазы — модификатор статов, растущий по формуле от времени), **без верхней границы по времени**
- Фаза выживания **сама не заканчивается**; в HUD показывается время выживания (растёт вверх), оно фиксируется как результат забега
- Точка входа для перехода на следующий уровень (`ZoneTimer.CompleteZone()`), которую вызовет портал убитого босса на этапе 6. До этапа 6 перехода нет (пока можно вызывать вручную для тестов)

**Definition of Done:** Игрок доживает таймер → начинается заметно отличающаяся фаза с духами, которая не заканчивается; духи ощутимо усиливаются с каждой секундой; в HUD виден счётчик времени выживания; переход на следующий уровень возможен только через вызов `CompleteZone()` (позже — портал).

**Статус:** реализовано и приведено к схеме выживания 2026-09-19 (переход по завершении фазы удалён).

---

## Этап 4 — Расширение контента: оружие, предметы, эволюции

**Цель:** Довести пул контента до объёма из GDD и реализовать систему эволюций.

**Задачи:**
- Довести число видов оружия до 15–20 (используя ScriptableObject-паттерн из Этапа 2)
- Реализовать разные типы поведения оружия: melee, projectile, orbit, area, boomerang (см. раздел 5.1 GDD)
- Систему уровней прокачки для каждого оружия/предмета
- Систему эволюций: условие "оружие макс. уровня + предмет-условие" → появление эволюционной карточки на левел-апе
- Составить и захардкодить (в данных, не в коде) первую таблицу соответствий оружие→предмет→эволюция для 4–5 пар, чтобы проверить систему end-to-end

**Definition of Done:** Есть минимум 15 оружий, минимум 4–5 рабочих эволюций, которые триггерятся согласно условиям и заметно меняют оружие визуально/по эффекту.

---

## Этап 5 — Враги: разнообразие и масштабирование сложности

**Цель:** Наполнить бестиарий и выстроить формулу роста сложности от времени забега.

**Задачи:**
- Реализовать 3–5 архетипов поведения врагов (танк/медленный, быстрый/слабый, дальнобойный и т.д.)
- ScriptableObject-описание врагов (статы, поведение, визуал) для лёгкого добавления новых
- Формула масштабирования HP/урона/скорости врагов от прошедшего времени забега — вынести коэффициенты в конфиг для последующей балансировки
- Плотность и частота спавна тоже растут по времени согласно той же логике

**Definition of Done:** Ощутимая разница в сложности между 1-й и 10-й минутой забега, несколько разных силуэтов/паттернов врагов на экране одновременно, без просадки FPS при большом количестве сущностей (см. Этап 9 про профилирование, но базовая пригодность нужна уже тут).

---

## Этап 6 — Боссы и система алтарей

**Цель:** Реализовать вызов боссов через алтарь, как описано в GDD (боссы не спавнятся автоматически).

**Задачи:**
- Объект "алтарь" на карте (пока можно на фиксированной тестовой позиции, до этапа процедурной генерации)
- Взаимодействие игрока с алтарём (подойти + активировать)
- Спавн босса при активации, с уникальным набором атак/паттерном поведения
- UI-индикация: HP-бар босса, визуальное выделение алтаря на карте/миникарте (если миникарта уже есть, иначе — направляющий индикатор)
- Реализовать минимум одного полноценного босса как референс для дальнейшего наполнения
- **Портал на следующий уровень:** после убийства босса на месте его появления (точка спавна) возникает портал; вход игрока в портал вызывает `ZoneTimer.CompleteZone()` и переводит на следующий уровень. Это единственный способ перехода дальше (фаза духов и таймер переход не открывают)
- Визуальный индикатор портала (направляющая стрелка, если он вне экрана) и запрет повторного вызова босса, пока портал не использован (решить при реализации)

**Definition of Done:** Игрок может завершить забег вообще не встретив босса (если не подошёл к алтарю; тогда он просто выживает против духов как можно дольше), либо найти алтарь, вызвать босса и сразиться с ним; после убийства босса на его месте появляется портал, через который игрок попадает на следующий уровень; бой с боссом ощущается как отдельное событие, а не как ещё один обычный враг.

---

## Этап 7 — Процедурная генерация карт и множественные миры

**Цель:** Заменить фиксированную тестовую сцену на процедурно генерируемые уровни, добавить структуру миров.

**Задачи:**
- Система процедурной генерации тайловой карты для уровня (алгоритм — на усмотрение реализации: chunk-based генерация вокруг игрока)
- Размещение алтаря босса процедурно на карте (гарантировать достижимость); портал появляется на месте призыва босса, поэтому вокруг алтаря нужна свободная площадка
- Параметризация генерации по миру (тайлсет, препятствия, плотность декораций) через ScriptableObject/конфиг мира
- Реализовать структуру минимум 1 полноценного мира с уникальным тайлсетом и врагами, затем растиражировать паттерн на оставшиеся 2–4 мира из GDD (3–5 миров суммарно)
- Экран выбора мира/уровня (базовый UI, без финальной полировки)

**Definition of Done:** Можно выбрать мир из меню, попасть на процедурно сгенерированную карту этого мира с соответствующим визуалом/врагами/боссом, пройти таймер зоны и отсчёт с духами на этой карте.

---

## Этап 8 — Мета-прогрессия и сохранения

**Цель:** Реализовать разблокировки между забегами и персистентное сохранение прогресса.

**Задачи:**
- Система сохранения (локальная, PlayerPrefs или файл — выбрать единый подход для всего проекта)
- Хаб/главное меню с экраном персонажей: показ заблокированных/разблокированных, условия разблокировки
- Аналогичный экран для оружия, если оно тоже открывается через мета-прогрессию
- Логика проверки условий разблокировки по итогам забега (например: пройти N уровней, убить конкретного босса, набрать N килов за забег)
- Стартовый набор условий разблокировки для 2–3 дополнительных персонажей как референс

**Definition of Done:** Пройденный забег с выполненным условием реально разблокирует нового персонажа, который сохраняется между запусками игры и доступен для выбора.

---

## Этап 9 — Оптимизация под мобильные устройства

**Цель:** Обеспечить стабильный FPS на бюджетных Android-устройствах при большом количестве объектов на экране.

**Задачи:**
- Профилирование (Unity Profiler) на пиковых сценариях (максимум врагов/снарядов/партиклов одновременно)
- Полный аудит object pooling (враги, снаряды, гемы, партиклы, любые instantiate/destroy в рантайме)
- Внедрение spatial partitioning (grid) для коллизий, если ещё не сделано в Этапе 1
- Оптимизация батчинга спрайтов (sprite atlases), проверка draw calls
- Тест на реальном или эмулируемом low-end Android устройстве, фиксация целевого FPS (например, 60 на средних устройствах, минимум 30 на low-end)

**Definition of Done:** На целевом low-end устройстве (или его профиле в эмуляторе) игра держит согласованный минимальный FPS в пиковых сценах с духами/боссом.

---

## Этап 10 — Полный UI/UX согласно GDD

**Цель:** Довести интерфейс до полного набора экранов из раздела 11 GDD.

**Задачи:**
- Главное меню, выбор персонажа/мира, настройки — довести до финального вида
- HUD в забеге: HP, XP, таймер зоны/отсчёта, килы, уровень — полировка расположения под ландшафтный мобильный экран
- Экран результатов забега со статистикой (включая **время выживания**) и полученными разблокировками
- Экран ежедневных заданий (см. Этап 11)
- Экран достижений (см. Этап 11)
- Экран настроек: громкость звука/музыки отдельно, переключение управления (задел под будущие PC-схемы, хотя в v1 только джойстик)

**Definition of Done:** Все экраны из раздела 11 GDD присутствуют, связаны навигацией без тупиков, читаемы в пиксель-арт стиле на целевом разрешении.

---

## Этап 11 — Дейли-квесты и достижения

**Цель:** Реализовать системы удержания игрока, заявленные в GDD как важные для v1.

**Задачи:**
- Система ежедневных заданий: ротация задач, трекинг прогресса, выдача наград, сброс по таймеру/дате
- Система достижений: список долгосрочных целей, трекинг прогресса, разовая выдача награды/фиксация в профиле
- Составить стартовый список конкретных дейликов и ачивок (закрыть open-вопрос из раздела 17 GDD)
- Подключить обе системы к сохранению из Этапа 8

**Definition of Done:** Ежедневные задания реально ротируются и засчитываются по игровым событиям, достижения открываются и сохраняются между сессиями.

---

## Этап 12 — Звук и музыка

**Цель:** Реализовать аудио-часть как полноценный приоритет, а не плейсхолдер (согласно GDD).

**Задачи:**
- Внедрить систему управления звуком (SFX-менеджер, музыкальный менеджер с кроссфейдом между треками миров)
- Расставить звуковые эффекты на ключевые события: удары оружия, смерть врагов, подбор гемов, левел-ап, начало отсчёта духов, призыв босса, победа/поражение
- Подключить уникальные музыкальные треки/лупы под каждый мир
- Прогнать полный забег с включённым звуком и убедиться в отсутствии рассинхрона/наложений

**Definition of Done:** Забег от начала до конца полностью озвучен согласно списку выше, звук регулируется в настройках отдельно для SFX и музыки.

---

## Этап 13 — Балансировка и полировка

**Цель:** Довести ощущение игры до финального качества перед релизом v1.

**Задачи:**
- Полный баланс-проход по всем оружиям, предметам, эволюциям, врагам, боссам (числа урона/HP/скорости)
- Проверка кривой сложности по всем 3–5 мирам от начала до конца
- QA-проход на реальных устройствах (минимум одно low-end и одно flagship Android/iOS устройство)
- Полировка анимаций, эффектов попаданий, отклика управления (game feel)
- Финальная проверка производительности (повторный профилинг после добавления всего контента)

**Definition of Done:** Игра проходится от начала до конца всеми доступными мирами без критичных багов, ощущается сбалансированной, держит целевой FPS.

---

## Этап 14 — Подготовка к порту на PC

**Цель:** Заложенный в архитектуре задел (Этап 0 и далее) реализовать как реальную поддержку PC.

**Задачи:**
- Добавить схему управления клавиатура+мышь параллельно джойстику (WASD/стрелки + автоатака остаётся, целиться по-прежнему не нужно)
- Адаптация UI под несенсорный ввод (наведение/клик вместо тапа) и под более широкие экраны/разрешения PC
- Проверка/адаптация под несколько aspect ratio (мобильный ландшафт был фиксирован, PC может требовать больше гибкости)
- Билд под Windows (и опционально Mac/Linux), базовый QA-проход на PC

**Definition of Done:** Игра запускается и полностью играбельна на PC с клавиатурой, без потери функциональности из мобильной версии.

---

## Примечания по ходу разработки

- Не переходить к следующему этапу, пока не закрыт Definition of Done текущего — это вертикальный, а не горизонтальный план
- Открытые вопросы из раздела 17 GDD (темы миров, таблица эволюций, формулы сложности, конкретные условия разблокировок, список дейликов/ачивок) нужно закрывать непосредственно перед этапом, где они становятся blocking (например, темы миров — перед Этапом 7, список дейликов — перед Этапом 11)
- Архитектурные решения на Этапе 0–2 (ScriptableObject-паттерны, object pooling, структура папок) не менять постфактум без необходимости — на них завязан весь дальнейший контент-пайплайн

---

## Статус реализации (обновляется по ходу работы)

- **Этапы 0–3: сделаны** (этап 3 пересмотрен 2026-09-19, см. выше).
- **Этап 4: сделан 2026-09-19.** 16 видов оружия + 5 эволюций, все 5 типов поведения (melee, projectile, orbit, area, boomerang), 5 уровней прокачки у каждого оружия, система эволюций «оружие макс. уровня + пассивка → карточка EVOLUTION!». Таблица оружие → предмет → эволюция хранится в данных (`UpgradePool.evolutions`, ассеты `EvolutionData`), не в коде:

| Оружие (макс. уровень) | Пассивка | Эволюция |
|---|---|---|
| Frying Pan | Hot Sauce | Flambe Pan |
| Throwing Forks | Espresso | Fork Storm |
| Spinning Spatulas | Sneakers | Spatula Tornado |
| Soup Puddle | Big Lunch | Soup Lake |
| Boomerang Bagel | Magnet Sock | Everything Bagel |

  Полный список оружия по типам: melee — Frying Pan, Baguette, Rolling Pin, Fly Swatter; projectile — Throwing Forks, Toothpick Gun, Ketchup Shotgun, Tomato Cannon; orbit — Spinning Spatulas, Pot Lids; area — Soup Puddle, Burp Shockwave, Salt Storm; boomerang — Boomerang Bagel, Frisbee Plate, Returning Ladle. Числа баланса стартовые (закрываются на этапе 13).

- **Этап 5: сделан 2026-09-19.** 5 архетипов врагов (данные в `EnemyData`, ассеты в `ScriptableObjects/Enemies`): Burnt Toast (обычный), Crumb Bug (рой пачками по 5, быстрый и слабый), Angry Bull Bun (рывок с красным телеграфом), Seed Spitter (держит дистанцию и стреляет), Armored Muffin (танк). Все коэффициенты сложности лежат в одном ассете `ScriptableObjects/DifficultyConfig` (рост HP/урона/скорости за минуту забега, надбавка за номер зоны, интервал и размер спавна, потолок численности), состав и открытие врагов по минутам — таблица `entries` на объекте `EnemySpawner`. Часы забега — `RunClock` (идут между зонами, стоят на паузе и при смерти).

  Стартовые значения (закрываются на этапе 13): множитель HP `1 + 0.20 × минуты` (×3 на 10-й минуте), урон `+8%` за минуту, скорость `+3%` за минуту (не выше ×1.8), плюс `+35%` за каждую следующую зону; спавн с 0.5 с (1 враг) до 0.2 с (3 врага) за 10 минут, потолок 120 → 370. Открытие: рой с 1:00, бык с 2:30, плевун с 3:00, танк с 4:00.

- **Этап 6: сделан 2026-09-19.** Алтарь (`Altar`), активация тапом/кликом по алтарю вблизи (кольцо-подсказка, издалека тап не работает), один раз за зону; стрелка-указатель на краю экрана (`OffscreenArrow`) появляется только после того, как игрок увидел алтарь; референсный босс **Waffle King** (`BossController`, в `Enemy.Active`, поэтому оружие его бьёт как обычного врага): фаза 1 — кольцо пуль, прицельные залпы, стомп с красным телеграфом; фаза 2 (ниже 50% HP) — быстрее, кольцо больше, призыв миньонов; HP босса масштабируется от времени забега и зоны через `DifficultyConfig`; HP-бар босса (`BossHealthBar`); после смерти босса **портал** на месте призыва (`Portal`), вход вызывает `ZoneTimer.CompleteZone()` → следующая зона, алтарь перемещается на 15–20 единиц от игрока, портал исчезает. Стартовая позиция алтаря в зоне 1 — фиксированная тестовая (28, 12); процедурное размещение — этап 7.

- **Этап 7: сделан 2026-09-19 (3 мира из 3–5).** Процедурная бесконечная карта чанками 16×16 вокруг игрока (`MapGenerator`, окно 7×7 чанков, детерминированно по сиду, тайлмап для земли, пул спрайтов для препятствий и декора). Каждый мир — ассет `WorldConfig` (тайлы, патчи земли, препятствия и декор с плотностью, таблица врагов, босс, множители сложности); каталог `WorldCatalog`. Миры: **Giant Kitchen** (Waffle King, текущий состав врагов), **Joke Graveyard** (Clown Zombie, Bat, Coffin Roller, Skeleton Jester, Tombstone Golem; босс Gravedigger Clown), **Office Hell** (Angry Intern, Paper Plane, Runaway Chair, Stapler Spitter, Filing Cabinet; босс Middle Manager Supreme). Главное меню с выбором мира (`MainMenu` → `Game`), кнопка MENU в забеге (временная до этапа 10). Каждая новая зона = новая карта (новый сид), игрок возвращается в (0,0), гемы и снаряды чистятся. Алтарь ставится процедурно на 26–36 единиц от старта. **Достижимость гарантирована конструктивно** (препятствия разнесены минимум на `obstacleGap`=1.4 друг от друга, включая границы чанков, поэтому они не могут замуровать область) и проверена обходом в ширину на 8 сидах (8/8). Игрок сталкивается с препятствиями (скользит вдоль них), враги их игнорируют. Миры 4–5 (например, «Sewer of Memes», «Space Pantry») добавляются новыми `WorldConfig` без кода.

- **Этап 8: сделан 2026-09-19.** Единый подход к сохранению — **JSON-файл** в `Application.persistentDataPath/rebonk_save.json` (`SaveSystem`, данные в `SaveData`: разблокировки, выбранный персонаж, суммарные и лучшие результаты, убитые боссы). Хаб-меню `MainMenu`: PLAY (выбор мира), CHARACTERS (карточки: закрытые силуэтом с условием и прогрессом, открытые выбираются и сохраняются), WEAPONS (коллекция: открыто/закрыто с условием). Забег ведёт `RunTracker` (убийства, зачищенные зоны, время выживания vs духи, убитые боссы): при смерти или выходе через MENU результат вливается в профиль, проверяются условия, новые разблокировки сохраняются и показываются на экране смерти («NEW UNLOCK: …»), там же кнопки PLAY AGAIN и MENU. Условия описываются данными (`UnlockInfo` на `CharacterStats` и `WeaponData`): типы `TotalKills`, `TotalRuns`, `BestKillsInRun`, `BestZonesCleared`, `BestSurvivalSeconds`, `BossDefeated`. Закрытое оружие не выпадает новой карточкой на левел-апе (персонаж может стартовать с ним).

  Стартовый набор разблокировок (закрывается на этапе 13 при балансе):

  | Что | Условие |
  |---|---|
  | Spud (стартовый) | открыт сразу |
  | Chef Chad (старт: Baguette, урон ×1.15) | убить 250 врагов за один забег |
  | Cookie Ranger (старт: Toothpick Gun, скорость 4.8, радиус подбора 2.6) | победить Waffle King |
  | Sir Tomato (старт: Tomato Cannon, HP 160) | продержаться 2:00 против духов |
  | Rolling Pin / Ketchup Shotgun / Frisbee Plate | 500 / 1000 / 2000 убийств суммарно |
  | Tomato Cannon | 150 убийств за забег |
  | Pot Lids / Returning Ladle | зачистить 1 / 2 зоны за забег |
  | Soup Puddle / Burp Shockwave | победить Gravedigger Clown / Middle Manager Supreme |
  | Salt Storm | продержаться 1:00 против духов |
  | Boomerang Bagel | сыграть 3 забега |
  | Открыты сразу: Frying Pan, Throwing Forks, Spinning Spatulas, Baguette, Fly Swatter, Toothpick Gun | |

  Известное ограничение: «Reset progress» в настройках появится с этапом 10 (метод `SaveSystem.DeleteSave()` уже есть).

- **Расширение контента (2026-09-19): +16 героев (всего 20) и +40 оружий (всего 56).** Оружие по назначению: ближнее (Near) — 18, дальнее (Ranged) — 21, по радиусу вокруг игрока (Radius) — 17. Новые режимы поведения: аура (`AuraWeapon`), самонаведение и отскок снарядов, спираль/кольцо/случайные направления, мины под игроком, мульти-импульсная нова, удары в обе стороны. Все 56 оружий проверены в Play на 5-м уровне (у каждого есть нанесённый урон, исключений нет). Экран оружия — прокручиваемая сетка с фильтром ALL / NEAR / RANGED / RADIUS.

  **Как открывается новое оружие.** Все новые виды закрыты и открываются мета-условиями (`UnlockInfo` на `WeaponData`), которые проверяются в конце забега (смерть или MENU): суммарные убийства, число забегов, убийства/уровень/зоны/время выживания за один забег, победа над боссами, число эволюций, суммарное время игры. Открытое оружие навсегда попадает в общий пул левел-апа (выпадает как карточка NEW). Лестница открытия идёт от «1 забег» и «100 убийств» до «15000 убийств суммарно» и «8 боссов».

- **Алтарь, предметы и тотемы (2026-09-19).** Алтарь: случайное направление и дистанция 28–85 (`MapGenerator.altarDistance`). Предметы: `PassiveData` расширен (второй стат для компромиссов), 20 новых «книг» + 5 старых = 25; новые статы в `StatType` (плоское HP, регенерация, броня, вампиризм, доп. снаряды, крит, множитель крита, область, скорость снарядов, пробитие, отскок, длительность, опыт, уклонение, доп. карточка, доп. жизнь) собираются в `PlayerStats`. Тотемы: `Totem` (5 с в радиусе 3.2), расстановка в `MapGenerator` (15–20 на зону, зоны очистки от препятствий, детерминированно по сиду), `BlessingData` / `BlessingPool` (14 благословений), выбор из 3 в `LevelUpScreen` (очередь: тотемы раньше левел-апов), счётчик `TotemHud`. Проверено в Play: алтарь на 40 картах 32–82 единиц во все стороны, тотемов 15–20 на каждой из 40 карт без нарушений расстояний, активация и сгорание прогресса, благословение +10% урона, все 25 предметов меняют заявленные статы, вампиризм/доп. снаряды/пробитие/отскок/уклонение/доп. жизнь/4-я карточка работают, баланс карточек 50/50.

- **Проверка читаемости UI (2026-09-19).** Аудит всех HUD и меню (временный сканер: обрезка, наслоение, выход за экран, мелкий шрифт) на 1280×720, 2340×1080 (19.5:9) и 1600×1200 (4:3). Исправлено: `CanvasScaler` в режиме `Expand` (ничего не обрезается на любых пропорциях), `SafeAreaFitter` (вырез/жест-бар: HUD и панели меню лежат в безопасной зоне, стрелки `OffscreenArrow` тоже), корень `Canvas/Hud` с `HudVisibility` — HUD скрывается за любым модальным окном (левел-ап, тотем, пауза, конец игры), длинные баннеры/таймер/список разблокировок автоподгоняют шрифт, двухстрочные записи оружия. Новое окно **PauseScreen** (кнопка PAUSE в углу): RESUME / RESTART RUN / MAIN MENU, для выхода и рестарта нужно второе нажатие (3 с), в окне сводка забега; кнопка MENU старого образца удалена.
- **Локализация (2026-09-19): 5 языков — English, Русский, Español, Deutsch, Português (BR).** `Rebonk.Core.Loc`: ключом служит сам английский текст (`Loc.T("PLAY")`, `Loc.F("Lv {0}", n)`), переводы лежат в `Assets/_Project/Resources/Localization/{ru,es,de,pt}.txt` (строки `English=>Перевод`, `\n` — перенос; нет строки → показывается английский, ничего не ломается). Язык хранится в `PlayerPrefs` (`rebonk.language`), по умолчанию — язык системы (uk/be → русский). Компонент `LocalizedText` вешается на статичные подписи сцен; динамические тексты (карточки, баннеры, условия разблокировки, имена оружия/героев/боссов/миров, описания и тексты уровней) переводятся через `Loc.T/F` в коде. Условия разблокировки переписаны без множественных чисел («Enemies killed in total: 250»), чтобы не тащить правила плюрализации. Кнопка выбора языка — в правом нижнем углу главного меню (по кругу), полноценное окно настроек — этап 10. Переведено ~270 строк ×4 языка. Шрифт — встроенный (`LegacyRuntime`): латиница, кириллица и диакритика покрываются; CJK/арабский потребуют отдельный шрифт. Аудит читаемости повторён для всех 5 языков (меню, 2330 привязок карточек, баннеры, пауза, экран смерти) — без обрезки и наслоений. **Как добавить новый текст:** написать английский вариант в коде/данных и добавить строку `English=>…` в четыре файла. **Как добавить язык:** значение в `Language`, файл `<код>.txt`, код и имя в `Loc`.

- **Этап 9: сделан в объёме, доступном без устройства (2026-09-19).** Замеры в редакторе на пиковой сцене (10-я минута, ~200–290 врагов + 6 видов оружия на 5-м уровне): среднее ~4.3 мс/кадр (230 fps), из них сама игровая логика ≈1.1 мс; кадр игровой камеры рисуется **~9 draw-вызовами** (тайлмап + 4 батча SRP Batcher + UI), проверено Frame Debugger. **Аудит пулов:** враги, снаряды (свои и вражеские), гемы, зоны — в пулах; `Instantiate/Destroy` в рантайме остались только на редких событиях (оружие при взятии, босс, портал, тотемы на старте зоны); аллокаций в игровом цикле не найдено (замер `GC Allocated In Frame` с отключением систем по одной: мусор даёт редактор, не игра). **Spatial grid не внедрён сознательно:** запросы к врагам — линейный проход по `Enemy.Active`, при ~300 врагах стоит доли миллисекунды; вернуться к вопросу, если на реальном устройстве профайлер покажет узкое место (задел: все запросы идут через `Enemy.Active`/`EnemyQuery`). **Что изменено:** (1) стриминг карты — новые чанки грузятся по **одному за кадр** (ближние первыми), выгрузка тоже по одному, а не целой строкой при пересечении границы чанка (это давало хитч ~5 мс в редакторе ≈ 30–40 мс на слабом телефоне); новая зона грузится сразу целиком; (2) `MobileSettings`: 60 FPS (на Android по умолчанию 30), без vSync, экран не гаснет; (3) URP: HDR выключен; (4) Sprite Atlas V2: `Atlas_Gameplay` (спрайты, оружие, миры) и `Atlas_UI` (иконки, UI) в `Art/Atlases`, ASTC 4×4, point-фильтр, без mipmap. Ориентация только ландшафт, IL2CPP + ARMv7/ARM64, инкрементальный GC — уже были. **Не сделано (нужно устройство):** реальный прогон на low-end Android и фиксация целевого FPS — для этого нужен подключённый телефон/эмулятор; после появления настоящих спрайтов повторить замер draw-вызовов и размера атласов.

- **Этап 10 (часть 1, 2026-09-19): настройки и результаты.** Окно **Settings** в главном меню (кнопка SETTINGS справа внизу; `SettingsScreen`): выбор языка кнопками (5 языков, текущий подсвечен), ползунки музыки и звуковых эффектов (`GameSettings`, хранятся в `PlayerPrefs`, отдельно от сохранения прогресса; подключение к звуку — этап 12), **RESET PROGRESS** с подтверждением вторым нажатием (стирает сохранение). В HUD появился счётчик убийств (`KillsHud`, под счётчиком тотемов). Экран смерти стал экраном результатов: убийства, зоны, время против духов, уровень, время забега, строка «NEW RECORD: …» при побитом рекорде (убийства / время выживания / уровень) и список новых разблокировок; заголовок GAME OVER больше не обрезается кириллицей. Остаётся для этапа 10: экраны ежедневных заданий и достижений (появятся вместе с этапом 11), переключение схемы управления (пока только джойстик), финальная полировка в пиксель-арте после появления настоящей графики.

- **Этап 11 (2026-09-19): ежедневные задания и достижения.** `DailyQuests` — 3 задания в день из фиксированного пула (13 шаблонов), выбор детерминирован датой (локальная полночь = ротация), метрики разные (убийства, зоны, боссы, эволюции, время против духов, уровень, забеги). Прогресс засчитывается по окончании забега (`RunTracker.FinishRun`), хранится в `SaveData` (`dailyDate/dailyPick/dailyProgress/dailyDone`). `Achievements` — 22 достижения, каждое = `UnlockInfo` по общим счётчикам сохранения (хранятся только id полученных). Награда = прогресс к условиям разблокировок: новые `UnlockType.DailyQuestsCompleted` и `AchievementsEarned` (пока ни одно оружие/персонаж их не использует — задать в MetaCatalog по желанию). Экраны QUESTS и ACHIEVEMENTS в главном меню, итоговый экран показывает QUEST DONE / ACHIEVEMENT. Строки добавлены во все 4 файла локализации.

- **Этап 12 (2026-09-19): звук и музыка — система готова, аудио пока заглушки.** `Core.Sound` (пул из 16 голосов, кроссфейд музыки, приглушение на паузе, ограничитель частоты и случайный питч), `SoundBank` (Resources/Audio) со слотами на 19 SFX и треки музыки, `GameAudio` (события забега), `UiClick` на всех кнопках, `MenuAudio`. Музыка на мир (`WorldConfig.zoneMusic/survivalMusic/bossMusic`), смена: зона → духи → босс → обратно, тишина при смерти. Ползунки Settings работают. Заглушки синтезированы (генератор удалён); замена — файлами с теми же именами, см. `Audio_Pipeline.md`. Живое прослушивание на слух и полный забег с настоящим аудио — за пользователем.

- **⚠ Не забыть (Этап 12):** настоящих аудиофайлов пока нет, в игре заглушки. Добавить позже (инструкция — `Audio_Pipeline.md`), затем прослушать полный забег и подогнать громкости. Напомнить перед релизом.

- **Этап 13, часть 1 — баланс (2026-09-19).** Без реального устройства сделано то, что можно измерить в редакторе (`Balance_Tools.md`): бот-игрок (25 забегов) и стенд урона по всем 56+ оружиям. Что изменено: (1) враги: HP базового/стажёра/клоуна 20/24→18 (стартовое оружие 10 урона убивает за 2 удара, а не за 3); (2) `DifficultyConfig`: старт спавна 0.5→0.8 с, стартовый лимит живых 120→60, рост 25→30/мин (в первую минуту раньше было 120 врагов и почти ноль убийств); (3) игрок получает 0.35 с неуязвимости после удара (мигает; `PlayerController.hitGraceSeconds`) — толпа не убивает мгновенно; (4) урон оружия по стенду: усилены Baguette ×1.6, Chopsticks ×1.6, ButterKnifeFlurry ×1.8, PepperGrinder ×1.5, PopcornPopper ×1.6, CheeseWheelRoll ×1.3, SpinningSpatulas ×1.4, SprinkleShower ×1.5, GarlicCloud ×1.4, FrostFreezerNova ×1.3; ослаблены зоны, чьи наложенные зоны били одну цель в 3–10 раз сильнее остальных: SoupPuddle ×0.6, MilkSplash ×0.4, FondueFountain ×0.4, SoupLake ×0.4, FrisbeePlate ×0.75. Персонажи (20) по бот-прогонам в одном диапазоне, сломанных нет.
- **⚠ Этап 13 не закрыт:** нужны человеческие проверки — боссы (кто и за сколько убивает, честность атак), кривая сложности по 3 мирам на живом прохождении, feel управления, QA на реальных устройствах (low-end + flagship), финальный профилинг после контента. Бот для этого слишком слаб.

- **Релиз в Google Play (2026-09-20):** пакет `com.Rebonk` (как в Play Console), minSdk 28 (Android 9+), формат AAB, IL2CPP ARMv7+ARM64, символы Public. Upload-ключ создан (`C:\Users\andre\RebonkKeys\`, вне репозитория), пароли в Unity вводит пользователь. Осталось: пароли в Publishing Settings → сборка AAB → загрузка в Play Console (внутреннее тестирование). Не хватает для листинга: иконка приложения, скриншоты, feature graphic 1024x500, политика конфиденциальности, форма Data safety, рейтинг контента.

- **AAB v1.0 собран (2026-09-20):** `Builds/Rebonk_1.0.aab` (61 МБ), подписан upload-ключом (CN=Rebonk), пакет `com.Rebonk`, versionCode 1, minSdk 28, ARMv7+ARM64, иконка — призрак с мечом (Default Icon). Символы для Play: `Builds/Rebonk_1.0-1.0-v1-IL2CPP.symbols.zip`. Для каждого следующего аплоада поднимать `AndroidBundleVersionCode`. Реальная проверка на устройстве и заполнение листинга в Play Console — за пользователем.

- **Статус на 2026-09-20 (конец сессии):** AAB/APK v1.0 собраны, AAB в внутреннем тестировании Google Play. Отключены заставка Unity и «Made with Unity» (нужна пересборка, versionCode 2). Открыто: имя разработчика в политике конфиденциальности + хостинг, скриншоты/графика для листинга, тест на реальном телефоне, анимации персонажей (процедурные → покадровые), человеческий плейтест баланса, этап 14.

- **Бесконечные улучшения + рост врагов (2026-09-20, по фидбеку с тестов).** Раньше карточки кончались (~55 повышений уровня: 6 оружий × 5 ур. + 6 книг). Теперь после максимума карточки не исчезают: оружие и «обычные» книги (maxLevel ≥ 5) улучшаются без предела (проверено до 1000 подряд). Оружие: +15% урона и +2% области (до +50%) за каждый уровень сверх максимума (`Weapon.EndlessDamagePerLevel`), книги растут линейно (`valuePerLevel × уровень`); отрицательный «минус» книги замораживается на максимуме. Особые книги (жизни, доп. карточки; maxLevel < 5) остаются с лимитом. Бесконечные карточки дополняют выбор, только когда обычных не хватает; эволюционировавшее оружие тоже растёт. Чтобы бесконечный рост не ломал игру, добавлены потолки: скорость атаки ×8, площадь ×6, скорость игрока 12, доп. снаряды 12, пробитие 20, отскоки 15, кража здоровья 30%, скорость снарядов ×5, длительность ×6, уклонение 60% (было). Броня режет не более 75% удара (минимум 25%). Враги: рост HP/урона теперь ускоряется со временем (квадратичный член: `hpQuadraticPerMinute` 0.015, `damageQuadraticPerMinute` 0.008; урон +10%/мин вместо 8%) и по зонам множитель compound ×1.4 за зону (было +35% линейно). Пример зона 1: на 10-й мин HP ×4.5/урон ×2.8, на 20-й ×11/×6.2, на 30-й ×20/×11; зона 3 вдвое выше. Новые строки во все 4 файла локализации. Нужна пересборка AAB (версия 3).

- **Ограниченная карта (2026-09-20, по фидбеку: алтарь было трудно найти на бесконечной карте).** Зона теперь квадрат 320×320 клеток (`MapGenerator.arenaHalfChunks = 10` → ±160, 0 = бесконечная карта; было 224→512→288→320, финально по запросу пользователя); алтарь (38–125 от старта) и тотемы (8–147, шаг между ними 19) распределены по всей арене, расстояния пропорциональны размеру. Игрок не может выйти за границу (`ObstacleMap.ClampToArena`, применяется в `Resolve`), за границей блоки карты не создаются, снаружи стоит тёмная стена шириной 26 клеток (видна с любого места у края). Враги и духи не спавнятся за картой: точка спавна за границей зеркалится на противоположную сторону от игрока (`ObstacleMap.ArenaSpawnPoint`). Проверено в Play: 74 врага у угла карты — ни одного за границей. Новая зона (после портала) строит новую арену заново.

- **AAB 1.5 (versionCode 3) собран (2026-09-20):** `Builds/Rebonk_1.5_v3.aab`, подпись upload-ключом, манифест проверен (без фильтра PVRTC, minSdk 28). Описание обновления — `ReleaseNotes_1.5.txt`.

- **Анимация ходьбы — код готов (2026-09-22).** `CharacterStats`: поля `framesDown/framesUp/framesSide` + `animFps` (направление выбирается по доминирующей оси движения, side отражается по X). `Enemy`: поле `walkFrames` + `animFps` (один ракурс, лево/право — существующий флип). Пока поля пустые — поведение как раньше (статичный спрайт), проверено в Play. Дальше — чисто арт-работа: промты и рекомендация по программе (Aseprite/Piskel) в `Art_Pipeline.md`.

- **Миникарта (2026-09-22).** Квадратная мини-карта в HUD под кнопкой паузы (150×150), следит за игроком с большим радиусом обзора (55 клеток), чем игровая камера; счётчики тотемов/убийств сдвинуты ниже под неё. Тап открывает полную карту (правая часть экрана, ~30% ширины, `MinimapExpanded`), пауза игры и затемнение остального HUD на время просмотра (как у паузы/левел-апа), закрывается кнопкой «Назад» или тапом по затемнению. Туман войны: `MinimapModel` (Gameplay) хранит одну текстуру 128×128 на всю (теперь ограниченную) арену; цвет местности запекается в неё один раз при подгрузке чанка (`WorldConfig.backgroundColor` + затемнение под препятствиями), а раскрытие — альфа-канал, проявляемый кругом вокруг игрока по мере ходьбы (радиус 16 клеток). Сбрасывается заново при каждой новой зоне (`MapGenerator.BuildZone`), то есть и при смерти/новом мире тоже (сцена перезагружается). Метки — простые цветные квадраты (плейсхолдер, без арта): игрок белый с чёрной обводкой, алтарь золотой, портал зелёный, босс красный крупный, тотемы синие (серые — уже использованные), враги мелкие красные точки (не более 40/60 одновременно, пул без аллокаций). **Важно: метки/местность показываются только там, где туман уже раскрыт** — простое попадание в радиус камеры недостаточно (проверено и исправлено по фидбеку). `Altar` получил `Current`/`Discovered` (static), `Totem.All` уже был.

- **Фон главного меню подставлен (2026-09-22).** `Art/UI/background_menu.png` (1672×940, от пользователя, конвертирован из .webp через canvas в браузере — Unity этого проекта не импортирует .webp напрямую). Добавлен как `MainPanel/Background` (первый по порядку — под всеми остальными элементами), растянут на весь экран. Кнопки/заголовок пока старые (плоские прямоугольники, стандартный шрифт) — фон уже ощутимо преображает вид. Следующий шаг из плана «расписного меню»: текстура-табличка для кнопок (9-slice) и шрифт.

- **Логотип REBONK подставлен (2026-09-22).** `Art/UI/logo.png` (771×390, от пользователя, PNG с фейковым «шахматным» фоном без реальной альфы — убрал сам через System.Drawing по цветовому порогу, светло-серый/белый фон стал настоящей прозрачностью). Заменяет текстовый `Title` (тот же GameObject, теперь `Image` вместо `Text`), увеличен на 15% по запросу (432×218), `SelectedLabel` сдвинут ниже, чтобы не наезжать.

- **Кнопки-таблички готовы (2026-09-22).** Из присланного пользователем референса (3 кнопки с баками иконка+текст) извлёк только иконки (play/person/sword, `Art/UI/Icons/icon_*.png`, вырезаны из растра по яркостному порогу через PowerShell/System.Drawing — в референсе текст был впечён в картинку, для 5-язычной локализации это не подходит) и сгенерировал сам два чистых 9-slice фона (`Art/UI/button_gold.png`, `button_navy.png`, скруглённый прямоугольник с плавным градиентом и обводкой (сначала была ещё тёмная полоса-бевел снизу — убрал по фидбеку, смотрелась как шов), PPU переопределён на 100 + border 24/32/24/24 для корректного 9-slice). Применены ко всем 6 кнопкам главного меню: ИГРАТЬ (золото) + 5 остальных (тёмно-синий); иконки — только на ИГРАТЬ/ПЕРСОНАЖИ/ОРУЖИЕ (для настроек/заданий/достижений своих иконок пока нет). Текст кнопок остался обычным `Text`+`LocalizedText` поверх — локализация не пострадала.

- **Шрифт для кнопок — откачен (2026-09-22).** Пробовал Titan One (Google Fonts, кириллица проверена заранее рендером через System.Drawing) — по фидбеку пользователя выглядело плохо, вернул исходный `LegacyRuntime` на всех текстах главного меню (важный нюанс: `Text.font = null` НЕ восстанавливает системный шрифт, а делает текст невидимым — нужно `Resources.GetBuiltinResource<Font>("LegacyRuntime.ttf")`). Файлы шрифта удалены из проекта.
- **Подпись «Играем за» сделана заметнее (2026-09-22, осталось после отката шрифта).** `SelectedLabel`: размер 32→38 (best-fit 20-38), добавлены `Outline` (чёрная, 90%) и `Shadow` (чёрная, 60%) — читается поверх пёстрого фона меню.
- **Экран персонажей переделан в стиле расписного меню (2026-09-22).** По присланному мокапу (пользователь готовит собственные картинки под него отдельно). Новая структура вместо одной карусели карточек: небольшая полоска миниатюр сверху (клик = только **превью**, не выбор), слева панель персонажа (имя/описание/HP/скорость/урон/особенность), по центру «диорама» с крупным портретом и пагинацией (пагинация умеет пропускать заблокированных персонажей без зацикливания), справа панель стартового оружия (урон/скорость атаки/область) и кнопка **ВЫБРАТЬ**, которая коммитит превью в сохранение (флип на «ВЫБРАНО», когда превью = уже выбранный). `CharacterCard.cs` упрощён до чистого превью-тумблера (только портрет/фон/замок/бейдж), вся логика панелей и пагинации — в новом `CharactersScreen.cs`; `HubMenu.cs` избавлен от старой carousel-логики. Табличный фон (`button_navy.png`, 9-slice) переиспользован для всех новых панелей/плашек; иконка замка нарисована процедурно. Новые строки локализации (5 языков) для подписей панелей и статов. Полностью протестировано в Play mode (пагинация, превью, выбор, сохранение). **Ждём от пользователя:** фон диорамы (прислал картинку ночного леса с замком — ждёт очереди на подключение) и иконки статов (сердце/скорость/меч) — сейчас статы текстовые без иконок.
- **Экран «Оружие и предметы» переделан в стиле расписного меню + добавлена система редкости (2026-09-22).** Общий экран для оружия И пассивных предметов (раньше предметы вообще не показывались в коллекции) с фильтрами ВСЕ/БЛИЖНИЕ/ДАЛЬНИЕ/РАДИУС/ПАССИВНЫЕ. Новое поле `Rarity` (Common/Rare/Epic/Legendary, с цветом и лейблом) добавлено в базовый `UpgradeData` — проставлено автоматически всем 56 оружиям + 5 эволюциям (по нормализованной сложности разблокировки, эволюции = Legendary) и 25 предметам (капы/уникальные = Legendary, компромиссные = Epic, редкие статы = Rare, базовые = Common) через одноразовый скрипт, значения лежат в самих ассетах. Карточка в сетке: иконка, имя, редкость (цветным текстом) + ряд точек-индикаторов редкости, короткая строка статов (или условие разблокировки для закрытых). Клик по карточке = превью в правой панели деталей (иконка крупно, имя, редкость, описание, 3 строки статов) — **без кнопки выбора**, т.к. оружие/предметы в игре не выбираются из меню, только открываются и выпадают на левел-апе. `WeaponEntry.cs` переписан под карточку сетки, новый `WeaponsScreen.cs` — контроллер экрана (аналог `CharactersScreen.cs`); `HubMenu.cs` избавлен от старой логики фильтров/RefreshWeapons. `MetaCatalog.Passives()` добавлен рядом с `Weapons()`. Новые строки локализации (5 языков): PASSIVE, Common/Rare/Epic/Legendary, заголовок экрана и т.д. Иконки предметов/оружия — уже настоящий арт пользователя (не заглушки), просто мелкие на карточках. Протестировано в Play mode (фильтры, прокрутка, превью, локализация детали).
- **У «Картошки» (Spud/Character_Hero) настоящий арт вместо заглушки (2026-09-22).** Пользователь прислал ZIP с 8 статичными позами (32×32, реальная прозрачность, без магента-фона) через свой пайплайн генерации. Задействованы 3 из 8 (юг/север/восток — под существующую систему анимации ходьбы Down/Up/Side; запад получается отражением востока, диагонали пока не используются, т.к. движок поддерживает только 3 направления). Файлы в `Art/Sprites/Characters/Spud/`, PPU переопределён на 16 (сорцы 32×32, при PPU16 персонаж выходит вдвое крупнее старого плейсхолдера — по фидбеку пользователя «слишком маленький, не понятно», изначально пробовал PPU32 для сохранения старого размера, но этого оказалось мало). `Character_Hero.sprite/framesDown/framesUp/framesSide` перепривязаны. Проверено в меню персонажей и в самой игре (Zone 1) — без ошибок.
- **У врага Crumb Bug (Enemy_Swarmer) настоящий арт + новая система поворота без анимации (2026-09-22).** Пользователь прислал ZIP с 8 статичными позами + полный цикл бега (8 кадров/направление); по фидбоку пользователя реализован **вариант без анимации движения** — только смена спрайта по направлению (юг/север/восток, запад = отражение), кадры бега не используются (импортированы и сразу удалены). Так как у `Enemy.cs` раньше была только одна плоская анимация ходьбы (`walkFrames`/`animFps`, общая на все направления, без различия верх/низ), добавлена новая система **`spriteDown`/`spriteUp`/`spriteSide`** (по образцу `CharacterStats.framesDown/Up/Side`, но статичная, без кадров) — `UpdateFacing()` выбирает спрайт по направлению к игроку (`dir`), зеркалит по X для движения влево; старый `UpdateWalkAnimation()` удалён вместе с полями. Это переиспользуемая инфраструктура для ЛЮБОГО врага (не только Swarmer) — просто заполнить 3 поля в инспекторе. Файлы в `Art/Sprites/Enemies/Swarmer/`, PPU=19 (сначала стоял PPU=38 — подобран так, чтобы совпадал физический размер со старым плейсхолдером 16×16, но пользователь попросил увеличить, снизил вдвое, как и с Картошкой).
- **Экран «Оружие и предметы» приведён к стилю экрана персонажей + фон (2026-09-22).** По фидбеку «выглядит не очень» — нашлась причина: плашки карточек и панели красились программным цветом (0.16,0.18,0.28) ПОВЕРХ уже раскрашенного спрайта `button_navy.png`, из-за чего всё уходило в почти чёрный вместо тёплого тёмно-синего (у персонажей плашки используют `color=white`, то есть натуральный цвет спрайта без затемнения). Поправлено: `WeaponEntry.cs` (карточки) и сцена (`RightPanel`, `IconBg`) — теперь `color=white`, подсветка выбранной карточки/вкладки — через лёгкий тёплый/зелёный тон вместо затемнения. Кнопка «Назад» и все 5 вкладок фильтра переведены с плоского `ui_white`-прямоугольника на скруглённую табличку `button_navy` (как у персонажей). Счётчик «Открыто X/Y» обёрнут в такую же табличку-пилюлю (был голым текстом без фона). **Важный урок процесса:** правки сцены, сделанные через `execute_code` ПОКА ИГРА В PLAY MODE, не сохраняются при выходе из Play — нужно останавливать Play mode перед любой правкой сцены, иначе работа откатывается (наступил на эти грабли повторно в этой сессии). Добавлен фон `background_weapons.png` (webp→PNG тем же способом через локальный http-server + canvas, см. ниже) — тот же ночной лес с костром/черепом/мечом, что просил пользователь, теперь виден в зазорах между панелями, как на MainPanel.
- **Фон для экрана персонажей тоже подставлен (2026-09-22).** `background_characters.png` (ещё один вариант той же ночной сцены — лес/замок/луна/красный флаг с черепом), добавлен в `CharactersPanel` тем же способом (первый child, растянут на весь экран, за диорамой/панелями). Диорама сохраняет свой собственный тёмный фон-«сцену» под портретом — новый фон виден только в зазорах вокруг.
- **Enemy_Charger (Angry Bull Bun) — настоящий арт (2026-09-22).** Тем же способом, что и Enemy_Swarmer: только статичные позы юг/север/восток (без анимации бега), PPU подобран сразу на ~2x крупнее старого плейсхолдера (PPU=15). Пользователь будет присылать арт на остальных врагов по одному — процесс отработан и повторяем (см. память проекта).
- **Мир 1 (Kitchen) — все 7 текстур земли/препятствий/декора заменены на настоящий арт (2026-09-22).** Пользователь сгенерировал `kitchen_ground_1/2.png`, `kitchen_patch_1.png`, `kitchen_jar.png`, `kitchen_bread_box.png`, `kitchen_crumb.png`, `kitchen_spill.png` по промтам из `Art_Pipeline.md` и прислал путь к папке на диске. Обработка (PowerShell/System.Drawing, новая техника — см. память): (1) magenta-фон убирается через **premultiplied alpha** — обычный чёрно-белый chroma-key с последующим `Graphics.DrawImage` даёт розово-фиолетовую кайму на краях объектов при таком сильном даунскейле (1254px → 8-32px), т.к. GDI+ мешает RGB соседних пикселей независимо от альфы; (2) для мелких декораций (crumb, spill) добавлена автообрезка по content-bbox (порог `alpha>0.6`, с запасом на цветной шум в фоне) — иначе объект размером ~150px в холсте 1254px превращался в почти невидимое пятно после сжатия до 8×8. Итог: банки, хлебницы, крошки, лужицы и два варианта пола с проступающими линиями затирки — всё видно в игре без артефактов. **Доработка по фидбеку пользователя (тем же вечером):** объекты (банки/хлебницы/крошки/лужицы) увеличены в 3 раза (`scale` в `WorldProp`, у банки и хлебницы пропорционально вырос и `collisionRadius`) — алтарь/тотемы/мобы не трогались, это отдельные системы. Финальный шаг сжатия текстур сменён с плавного на «ближайший сосед» — иначе при таком увеличении была заметна мутность. Исходники (сырые PNG 1254×1254 с промтов) на всякий случай сохранены в `DocsProject/SourceArt/Kitchen/` (не в `Assets/`, чтобы Unity не индексировал их как спрайты).
- **Все 5 врагов Office Hell + Enemy_Spirit — настоящий арт (2026-09-22).** Intern, Runaway Chair, Stapler, Filing Cabinet, Spirit заменены тем же способом (юг/север/восток, PPU подобран на ~2x старого размера). **Enemy_OF_PaperPlane НЕ заменён** — в присланном архиве `of_paper_plane.zip` лежит арт человечка, а не самолётика (не совпадает с собственным промтом в metadata.json архива — ошибка генерации на стороне инструмента пользователя, не моей обработки). Оставлен старый плейсхолдер, файлы из архива лежат в `Art/Sprites/Enemies/OFPaperPlane/` неиспользуемые, ждут решения пользователя. Из всех врагов трёх миров + Spirit не хватает только настоящего арта для paper_plane и 2 боссов (Gravedigger Clown, Middle Manager Supreme).
- **Экран выбора мира переделан в стиле остальных меню + по референсу (2026-09-22).** По просьбе «сделать так же как в других меню» + скрин-референс с 3 строками статов на карточке. Решения (обсуждались с пользователем): интерактивность оставлена как есть — клик по карточке сразу стартует забег, БЕЗ отдельного «выделения» центральной карточки и стрелок карусели с референса (это увеличило бы объём работы без функциональной необходимости при всего 3-5 мирах). Для 3 строк статов (ВРАГИ/ЛУТ/СЛОЖНОСТЬ) в `WorldConfig` добавлено новое поле `difficultyTier` (0-2, косметическое, как `Rarity` у оружия) — Kitchen=0, Graveyard=1, Office=2; все 3 строки на карточке идут в связке по этому уровню (Слабые/Обычный/Лёгкая → Средние/Хороший/Средняя → Сильные/Редкий/Высокая), с прогрессией цвета зелёный→жёлтый→красный. `WorldCard.cs` переписан (добавлены 3 поля Text для строк статов); `WorldSelectScreen.cs` (компонент на `Canvas`, НЕ на `WorldPanel` — важно при поиске) получил бейдж «Открыто X/Y» (всегда показывает «все миры открыты», т.к. в игре нет системы блокировки миров). Карточки и кнопка «Назад» переведены на табличку `button_navy` (было — плоский `ui_white`, как и у не обновлённых ранее экранов). **Грабли, на которые наступил повторно:** делал правки сцены прямо в Play mode дважды подряд в этом заходе — оба раза терялись при выходе, пришлось переделывать в Edit mode.
- **Новый способ конвертации webp→PNG: локальный http-server вместо file://.** Встроенный браузер отказывается выполнять JS на `file://` страницах вне зависимости от того, лежит ли файл внутри рабочей папки проекта («files outside the project folder render as static snapshots» — ограничение относится к file:// в целом, а не к конкретному пути). Рабочий способ: скопировать картинку во временную папку внутри проекта, поднять статик-сервер через `.claude/launch.json` (`npx http-server <папка> -p <port>`) и `preview_start`, открыть `http://localhost:<port>/file.webp`, дальше — обычная техника canvas→dataURL→(node декодирует base64 в PNG). См. также [[png-fake-transparency-fix]]. **Анимация бега (полноценные 8 кадров/направление) отложена на потом** — пользователь подтвердил, ZIP с кадрами бега пока не удалён из `F:\Загрузки`, можно переиспользовать позже, когда `Enemy.cs` получит поддержку многокадровой анимации per-direction.
